info:
  name: handleZoneEntry
  type: http
  seq: 4
  tags:
    - device-controller

http:
  method: POST
  url: "{{baseUrl}}/api/v1/devices/:macAddress/zone-entry"
  params:
    - name: macAddress
      value: ""
      type: path
  body:
    type: json
    data: |-
      {
        "zoneId": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/devices/:macAddress/zone-entry"
      method: POST
      params:
        - name: macAddress
          value: ""
          type: path
      body:
        type: json
        data: |-
          {
            "zoneId": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "success": false,
            "message": "",
            "data": "",
            "time": ""
          }
