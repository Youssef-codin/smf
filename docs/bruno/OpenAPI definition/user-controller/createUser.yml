info:
  name: createUser
  type: http
  seq: 5
  tags:
    - user-controller

http:
  method: POST
  url: "{{baseUrl}}/api/v1/users/"
  body:
    type: json
    data: |-
      {
        "username": "",
        "email": "",
        "password": "",
        "roles": []
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/users/"
      method: POST
      body:
        type: json
        data: |-
          {
            "username": "",
            "email": "",
            "password": "",
            "roles": []
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "success": false,
            "message": "",
            "data": "",
            "time": ""
          }
