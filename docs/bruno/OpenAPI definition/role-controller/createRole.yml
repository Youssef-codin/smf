info:
  name: createRole
  type: http
  seq: 5
  tags:
    - role-controller

http:
  method: POST
  url: "{{baseUrl}}/api/v1/roles/"
  body:
    type: json
    data: |-
      {
        "roleName": ""
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/roles/"
      method: POST
      body:
        type: json
        data: |-
          {
            "roleName": ""
          }
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "success": false,
            "message": "",
            "data": "",
            "time": ""
          }
