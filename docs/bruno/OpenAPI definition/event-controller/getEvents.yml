info:
  name: getEvents
  type: http
  seq: 2
  tags:
    - event-controller

http:
  method: GET
  url: "{{baseUrl}}/api/v1/events/client"
  params:
    - name: since
      value: ""
      type: query
      disabled: true
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: OK
    request:
      url: "{{baseUrl}}/api/v1/events/client"
      method: GET
      params:
        - name: since
          value: ""
          type: query
          disabled: true
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: "*/*"
      body:
        type: text
        data: |-
          {
            "success": false,
            "message": "",
            "data": "",
            "time": ""
          }
